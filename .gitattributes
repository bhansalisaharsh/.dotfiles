*.deskthemepack filter=lfs diff=lfs merge=lfs -text

# Handle text files automatically
* text=auto

# Explicitly force LF for certain files (e.g., shell scripts)
*.sh text eol=lf

# Force CRLF for specific files (e.g., Windows batch files)
*.bat text eol=crlf

